{
  "name": "ax-engineering-harness",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["harness"],
  "engines": { "node": ">=24 <27" },
  "scripts": {
    "build": "npm run build --workspace @ax-harness/core",
    "typecheck": "npm run typecheck --workspace @ax-harness/core",
    "test": "npm run test --workspace @ax-harness/core",
    "ax": "tsx harness/cli/index.ts",
    "check": "npm run typecheck && npm test && npm run build"
  }
}
